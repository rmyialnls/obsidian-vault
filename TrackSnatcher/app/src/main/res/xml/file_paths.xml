<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches File(context.cacheDir, "shared_images") used by ShareableMemoryCard. -->
    <cache-path name="shared_images" path="shared_images/" />
</paths>
